From 315dab1615a75e0288fa344a0240ff9e00f9c9d5 Mon Sep 17 00:00:00 2001
From: Instinct Agent <agent@instinct.com>
Date: Fri, 4 Sep 2026 10:49:29 +0700
Subject: [PATCH] docs: position agent-think-map as a visual debugger

---
 README.md             | 24 ++++++++++++------------
 apps/demo/index.html  | 12 ++++++------
 apps/demo/src/App.tsx |  5 ++---
 examples/drop-in.html |  4 ++--
 package.json          |  2 +-
 5 files changed, 23 insertions(+), 24 deletions(-)

diff --git a/README.md b/README.md
index 2521d13..21afcc6 100644
--- a/README.md
+++ b/README.md
@@ -1,12 +1,12 @@
 <p align="center">
-  <img src="docs/hero.png?v=2" alt="Live think-map: prompt, chain-of-thought, skill, tool call, MCP inspector" width="100%" />
+  <img src="docs/hero.png?v=2" alt="Visual agent debugger showing a prompt, execution path, tool calls, skills, MCP calls, and inspector" width="100%" />
 </p>
 
 <h1 align="center">agent-think-map</h1>
 
 <p align="center">
-  <strong>See the agent think</strong> — chain-of-thought, skills, tools, and MCP.<br/>
-  <strong>Model-agnostic.</strong> Same canvas in the chat UI you ship, or beside Claude Code and Codex.
+  <strong>The visual debugger for AI agents.</strong><br/>
+  See the execution path behind every answer - tools, skills, MCP calls, subagents, latency, and cost - live beside your coding agent or inside your product.
 </p>
 
 <p align="center">
@@ -156,8 +156,8 @@ If the studio is already running on port 3335, re-running `--install` still writ
 Four lines. Point `events-url` at your agent's SSE.
 
 ```html
-<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/agent-think-map@0.1.1/dist/styles.css" />
-<script type="module" src="https://cdn.jsdelivr.net/npm/agent-think-map@0.1.1/dist/element.cdn.js"></script>
+<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/agent-think-map@0.1.2/dist/styles.css" />
+<script type="module" src="https://cdn.jsdelivr.net/npm/agent-think-map@0.1.2/dist/element.cdn.js"></script>
 <agent-think-map events-url="/sse" layout="split"></agent-think-map>
 ```
 
@@ -195,7 +195,7 @@ No account. No API key. Replays a recorded GitHub-issue turn in the browser.
 | Node | Meaning |
 | --- | --- |
 | Prompt | What the user asked |
-| Thinking | Streaming chain-of-thought |
+| Thinking | Reasoning events, when the runtime exposes them |
 | Skill | On-demand instruction pack that loaded, and why |
 | Tool | Builtin call (`Read`, `Bash`, `Grep`, …) with a one-line reason |
 | MCP | `server / tool` — the Model Context Protocol hop |
@@ -220,13 +220,13 @@ You are left asking:
 - **Why** did the model pick that step?
 - Which **model**, and how many **tokens**?
 
-Logs are a transcript. You need the route — inside the chat you already ship, or beside Claude Code or Codex. Same map. Any model you can emit.
+Your logs show events. Agent Think Map shows the route - inside the chat you already ship, or beside Claude Code or Codex. Same map. Any model you can emit.
 
 ## What agent-think-map is
 
-An embeddable canvas. While the run happens, the graph grows:
+A visual debugger and embeddable execution UI. While the run happens, the graph grows:
 
-**prompt → thinking → skill → tool / MCP → answer**
+**prompt → reasoning (when exposed) → skill → tool / MCP → answer**
 
 Click a node. The inspector shows why it fired, the input, the output, and how long it took. The tape at the bottom is a **tool-call timeline** you can scrub.
 
@@ -236,7 +236,7 @@ If you need a hosted trace warehouse, use LangSmith. If you need the trace *besi
 
 ```mermaid
 flowchart LR
-  Prompt[Prompt] --> Think[Chain of thought]
+  Prompt[Prompt] --> Think[Reasoning when exposed]
   Think --> Skill[Skill replay]
   Skill --> Tool[Tool call]
   Tool --> Mcp[MCP inspector]
@@ -322,10 +322,10 @@ Optional explicit imports: `agent-think-map/claude`, `agent-think-map/openai`, `
 
 | You | What you get |
 | --- | --- |
-| Shipping a chat product | Users (and you) can **see the agent think** — any model you emit — instead of trusting a spinner |
+| Shipping a chat product | Users (and you) can inspect the agent execution path instead of trusting a spinner |
 | Using **Claude Code** in the terminal | The same map in the browser: sessions, model, tokens, the route — without leaving the CLI |
 | Using **Codex Desktop or CLI** | The same map: sessions, model, effort, skills, tools, and MCP as hooks fire — without changing your Codex workflow |
-| Debugging a runaway loop | A **live trace** of skills, tools, and MCP — not a 4k-line log |
+| Debugging a runaway loop | A **live trace** of skills, tools, and MCP that makes the route clear without a 4k-line log |
 | Teaching or demoing agents | A canvas that builds in real time. Replay from a fixture. No keys. |
 | Building on MCP / skills | First-class nodes, not another generic “function call” chip |
 
diff --git a/apps/demo/index.html b/apps/demo/index.html
index 167423e..b65cfac 100644
--- a/apps/demo/index.html
+++ b/apps/demo/index.html
@@ -3,16 +3,16 @@
   <head>
     <meta charset="UTF-8" />
     <meta name="viewport" content="width=device-width, initial-scale=1.0" />
-    <title>agent-think-map — See the agent think</title>
+    <title>agent-think-map - Visual debugger for AI agents</title>
     <meta
       name="description"
-      content="agent-think-map: see the agent think — live CoT, skills, tools, and MCP inside the chat you already ship. Not a dashboard. No API key."
+      content="Agent Think Map is the visual debugger for AI agents. Inspect tools, skills, MCP calls, subagents, latency, and cost inside your product or beside your coding agent."
     />
     <link rel="canonical" href="https://nimrodfisher.github.io/agent-think-map/" />
-    <meta property="og:title" content="agent-think-map — See the agent think" />
+    <meta property="og:title" content="agent-think-map - Visual debugger for AI agents" />
     <meta
       property="og:description"
-      content="agent-think-map is an embeddable canvas for chain-of-thought, skills, tools, and MCP inside the chat UI you already ship. No API key."
+      content="See the execution path behind every answer: tools, skills, MCP calls, subagents, latency, and cost. No API key."
     />
     <meta property="og:type" content="website" />
     <meta property="og:url" content="https://nimrodfisher.github.io/agent-think-map/" />
@@ -31,7 +31,7 @@
           {
             "@type": ["SoftwareApplication", "WebApplication"],
             "name": "agent-think-map",
-            "description": "Embeddable canvas that shows an agent's chain-of-thought, skills, tools, and MCP inside the chat UI you already ship — not a hosted tracing dashboard.",
+            "description": "Visual debugger and embeddable execution UI for agent tools, skills, MCP calls, subagents, latency, and cost.",
             "operatingSystem": "Web",
             "applicationCategory": "DeveloperApplication",
             "offers": { "@type": "Offer", "price": "0", "priceCurrency": "USD" },
@@ -51,7 +51,7 @@
                 "name": "What is agent-think-map?",
                 "acceptedAnswer": {
                   "@type": "Answer",
-                  "text": "An embeddable canvas that shows an agent's chain-of-thought, skills, tools, and MCP inside the chat UI you already ship. It is not a hosted tracing dashboard."
+                  "text": "A visual debugger and embeddable execution UI that shows the route behind an agent answer, including tools, skills, MCP calls, subagents, latency, and cost."
                 }
               },
               {
diff --git a/apps/demo/src/App.tsx b/apps/demo/src/App.tsx
index b0ee542..b7a8b97 100644
--- a/apps/demo/src/App.tsx
+++ b/apps/demo/src/App.tsx
@@ -13,9 +13,8 @@ export default function App() {
           <span className="atc-kicker">Live demo</span>
           <h1>agent-think-map</h1>
           <p>
-            See the agent think — embeddable tracing for chain-of-thought, skills,
-            tools, and MCP inside the chat UI you already ship. Not a hosted
-            dashboard. No API key.
+            The visual debugger for AI agents. See the execution path behind every
+            answer: tools, skills, MCP calls, subagents, latency, and cost. No API key.
           </p>
         </div>
         <button type="button" className="demo-replay" onClick={() => setEpoch((n) => n + 1)}>
diff --git a/examples/drop-in.html b/examples/drop-in.html
index 6f1712a..63c9483 100644
--- a/examples/drop-in.html
+++ b/examples/drop-in.html
@@ -6,7 +6,7 @@
     <title>agent-think-map drop-in</title>
     <link
       rel="stylesheet"
-      href="https://cdn.jsdelivr.net/npm/agent-think-map@0.1.1/dist/styles.css"
+      href="https://cdn.jsdelivr.net/npm/agent-think-map@0.1.2/dist/styles.css"
     />
     <style>
       html,
@@ -25,7 +25,7 @@
     <agent-think-map events-url="/sse" layout="overlay"></agent-think-map>
     <script
       type="module"
-      src="https://cdn.jsdelivr.net/npm/agent-think-map@0.1.1/dist/element.cdn.js"
+      src="https://cdn.jsdelivr.net/npm/agent-think-map@0.1.2/dist/element.cdn.js"
     ></script>
   </body>
 </html>
diff --git a/package.json b/package.json
index 815cb27..68c497e 100644
--- a/package.json
+++ b/package.json
@@ -2,7 +2,7 @@
   "name": "agent-think-map",
   "version": "0.1.1",
   "license": "MIT",
-  "description": "Live think-map beside Claude Code or Codex CLI, or an embeddable canvas for the chat UI you already ship — chain-of-thought, skills, tools, and MCP.",
+  "description": "Visual debugger for AI agents: inspect tools, skills, MCP calls, subagents, latency, and cost beside Claude Code or Codex, or inside your product.",
   "keywords": [
     "ai",
     "agent",
-- 
2.34.1

